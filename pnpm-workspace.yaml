---
# QBall Design System — pnpm workspace.
# v1 packages: @qball-inc/tokens (CSS/DTCG, zero build) + @qball-inc/react.
# The fixtures/consumer scratch harness adds its own
# glob when it lands; packages/* is sufficient for the initial scaffold.
packages:
  - "packages/*"
