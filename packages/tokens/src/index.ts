// Placeholder source so `tsc --noEmit` has an input on the empty scaffold tree.
// @qball-inc/tokens ships CSS + DTCG JSON with zero build step; WP-B-1.1 replaces
// this placeholder with the verbatim-extracted token surface (theme.css,
// tokens.json, components.css) and a Fira-fallback --font-display restore seam.
export {};
